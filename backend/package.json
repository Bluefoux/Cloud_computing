{
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development node core.js",
    "devserver": "dotenv -e .env.dev -- node core.js",
    "docker:redis": "docker-compose -f docker-compose.dev.redis.yml up -d",
    "start": "NODE_ENV=production node core.js",
    "docker:dev": "docker-compose -f docker-compose.dev.yml up",
    "docker:production": "docker-compose -f docker-compose.production.yml up --build"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/env": "^5.0.2",
    "@fastify/mysql": "^5.0.2",
    "chalk": "^5.6.2",
    "fastify": "^5.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "infrastructure",
    "template",
    "docker",
    "env",
    "node"
  ],
  "private": true
}
